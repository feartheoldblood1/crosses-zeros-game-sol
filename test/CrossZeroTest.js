const {expect} = require("waffle");
const {ethers} = require("hardhat");
const { isCallTrace } = require("hardhat/internal/hardhat-network/stack-traces/message-trace");

descirbe("CrossZerosGame", () => {
    let player1
    let player2;
    let height; 
    let width;

    descirbe("CrossZerosGame testing", () => {
        it("", async () => {

        });
    });
});